# Builtin reference

*This chapter specifies psh's builtin commands. Currently a stub —
content to be written.*

## TODO

- `.` / `source` — execute file in current scope
- `cd` — change directory
- `echo`, `printf` — output
- `read` — input from stdin
- `test` / typed predicates
- `true`, `false`
- `eval`
- `exec`
- `shift`
- `export` (as standalone command, complementing `let export`)
- `umask`, `ulimit`
- `command`, `builtin`, `whatis`
- `break`, `continue`
- `hash`
