# Invocation and startup

*This chapter specifies how psh behaves when invoked as a login
shell, interactive shell, or script executor. Currently a stub —
content to be written.*

## TODO

- Login shell detection (argv[0] convention, `-l` flag)
- Startup file sequence (system rc, user profile, interactive rc)
- Invocation flags (`-c`, `-s`, `-i`, `-l`, `-e`, `-x`)
- Environment inheritance (environ(7) → psh namespace)
- $0, positional parameters
